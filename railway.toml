[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn railway_production_bot:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30

[variables]
PYTHON_VERSION = "3.11"